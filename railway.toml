[build]
command = "rm -rf node_modules .npm 2>/dev/null; npm install --legacy-peer-deps --no-fund && npm run build && composer install --no-dev --optimize-autoloader"

[env]
PORT = "8080"

[deploy]
startCommand = "php -S 0.0.0.0:$PORT -t public"
