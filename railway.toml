[build]
command = "npm ci --no-audit --prefer-offline && npm run build && composer install --no-dev --optimize-autoloader"

[env]
PORT = "8080"

[deploy]
startCommand = "php -S 0.0.0.0:$PORT -t public"
