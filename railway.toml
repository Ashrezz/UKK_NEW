[build]
command = "rm -rf /app/node_modules/.cache 2>/dev/null; npm ci --no-fund --no-audit --force && npm run build && composer install --no-dev --optimize-autoloader"

[env]
PORT = "8080"

[deploy]
startCommand = "php -S 0.0.0.0:$PORT -t public"
